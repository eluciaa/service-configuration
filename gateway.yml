server:
  port: 8081
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://192.168.1.2:8761/eureka
  instance:
    prefer-ip-address: true
    ip-address: 192.168.1.2
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: bankaccount
          uri: lb://bankaccount
          predicates:
            - Path=/accounts/**
        - id: credit
          uri: lb://credit
          predicates:
            - Path=/credits/**
        - id: statusaccount
          uri: lb://statusaccount
          predicates:
            - Path=/status/**
        - id: customer
          uri: lb://customer
          predicates:
            - Path=/customers/**